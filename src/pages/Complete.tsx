import React from 'react';

import PageLayout from "components/PageLayout";

const Complete = () => {
  return (
    <PageLayout cancelableHeader={false}>
      {() => (
        <div>
          Complete
        </div>
      )}
    </PageLayout>
  );
};

export default Complete;
